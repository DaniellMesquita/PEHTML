gambling and exchange rating tags
religious parody rating tag
religious rating tag

Social profiles
Social buttons

P Kit

id in headings

<group> tag

update browsertab tag (in index and cst-tab)

noPEHTML tip: open as popunder, copy to clipboard

cst tab: open as popunder, title (in tab and in code), color (in tab and in code), background (in tab and in code), copy to clipboard

* Fix NOPEHTML blockquote
* Added W3counter (SITE);
* Site PEHTML score (SITE);
* PEHTML Requests (PAGE);
* PEHTML Scores (PAGE);
* Exclusive page for each tag (SITE);
* Fixed "custom tab" page (SITE);
* Tabs menu element (PEHTML);
* Alerts (PEHTML);
* Alert counter bubble (PEHTML);
* Sites working like apps, and can be downloaded as apps/clients (PEHTML);
* Window preview's buttons API (PEHTML);
* Navigation buttons in top bar (SITE);
* User buttons in top bar (SITE);
* Inline HTML | HTML+CSS tabs menu in "customtab.html" (SITE);
* User menus in top bar (SITE);
* Facebook popup (SITE);
* Google+ popup (SITE);
* Twitter popup (SITE);
* Ask.fm popup (SITE);
* Tumblr icon in footer bar (SITE);
* Pinterest icon in footer bar (SITE);
* Feed icon in footer bar (SITE);
* Peeper icon in footer bar (SITE);
* "customtab.html" in popup (SITE);
* Fix <nopehtml> popup (SITE);
* Peep button for user (SITE);
* <bar> element, ahead of <body> (PEHTML);
* Book API (PEHTML);
* Reading mode API (PEHTML);
* New PEHTML icon in topbar (SITE);
* Plasma breadcrumbs in topbar (SITE);
* Add wrappers to <h1> elements (SITE);
* Use Pagespeed's insights to increase speed/simplicity (SITE);
* Internet Boat links in footerbar (SITE);
* Developers site link in footerbar (SITE);
* P Developers forum link in footer bar (SITE);
* P UI Framework link in footerbar (SITE);
* Title of tab accourding to field in "custom tab" page (SITE);
* Title color of tab accourding to field in "custom tab" page (SITE);
* Tab icon link field in "custom tab" page (SITE);
* Icon of tab accourding to field in "custom tab" page (SITE);
* Color of tab accourding to field in "custom tab" page (SITE);
* <noscreens> tag (PEHTML);
* Warning of extention site need to download (PEHTML);
* Age ratings (PEHTML);
* Script that if are not in IB, redirect to other page - with bars and other deprecated tags (SITE);
* PHP site with MySQL, showing browsers using PHP - and his creators and base users (SITE);